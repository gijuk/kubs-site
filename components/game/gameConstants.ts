/** 게임 월드(논리 좌표) 크기. 캔버스는 이 크기로 그려진 뒤 CSS로 확대되어 표시됩니다. */
export const WORLD_W = 880;
export const WORLD_H = 270;
export const GROUND_Y = 214;

/** 호랑이의 고정 x 위치 (가로로는 움직이지 않습니다) */
export const TIGER_X = 72;

/** 장애물이 화면 오른쪽 바깥에서 생성되는 x 위치 */
export const SPAWN_X = WORLD_W + 20;
